import { CSSColor } from '@/types';

export type PACS_COLORS = {
  tableHeaderBackground: CSSColor;
  fieldDisabledBackground: CSSColor;
  uploadNonDicomBackground: CSSColor;
  borderColor: CSSColor;
  sidebarHeaderTitle: CSSColor;
  backgroundTable: CSSColor;
  backgroundPanelContent: CSSColor;
  backgroundEditor: CSSColor;
  borderSideHeaderColor: CSSColor;
  textSideHeaderColor: CSSColor;
  textSideHeaderHover: CSSColor;
  tableRowHoverColor: CSSColor;
  textTableRowHoverColor: string;
  textTableRowSelectedColor: string;
  iconDefaultColor: CSSColor;
  iconDynamicPanelInactiveColor: CSSColor;
  textIconHoverColor: string;
  backgroundButtonActiveColor: string;
  textButtonActiveColor: CSSColor;
  borderButtonColor: CSSColor;
  selectedItemSidebarColor: CSSColor;
  tableRowSelectedBackgroundColor: CSSColor;
  notchedOutlineInputColor: CSSColor;
  primaryTextColorNavbar: CSSColor;
  autoCompleteTagColor: CSSColor;
  text: {
    label: CSSColor;
    red: CSSColor;
    green: CSSColor;
    blue: CSSColor;
    gray: CSSColor;
    black: CSSColor;
    orange: CSSColor;
  };
  statistic: {
    background: CSSColor;
    title: CSSColor;
  };
};

export type QMS_COLORS = {
  tableHeaderBackground: CSSColor;
  modalityButtonBackground: CSSColor;
};
